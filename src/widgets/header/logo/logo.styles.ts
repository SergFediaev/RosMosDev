import styled from 'styled-components'

const Logo = styled.h1`
    &:hover {
        cursor: help;
    }
`

const Loading = styled.span`
    font-size: 2rem;
    font-weight: bold;
`

export const S = {
    Logo,
    Loading,
}
